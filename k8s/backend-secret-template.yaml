apiVersion: v1
kind: Secret
metadata:
  name: cloudlab-backend-secret
  namespace: cloudlab
type: Opaque
stringData:
  # Replace the values below with your Terraform outputs / secure values
  DATABASE_URL: "<POSTGRES_CONNECTION_STRING>"
  JWT_SECRET: "<STRONG_JWT_SECRET>"
  JWT_EXPIRES_IN: "7d"
